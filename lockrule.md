# 「아직 못 가는 지역」을 어떻게 판정하는가

이 자료실은 **APK 안의 마스터 데이터 전부**를 읽는다. 거기에는 아직 게임에
안 열린 것도 들어 있다. 그래서 «내가 지금 갈 수 있는 곳» 과 «데이터에만
있는 곳» 을 갈라야 한다. 그 규칙을 여기 한 곳에 적어 둔다.

판정은 **세 겹**이고, 위에서부터 우선한다.

## ① 세이브가 있으면 — 세이브가 진실이다

`GameData.Maps()` 에 들어간 지역만 세이브에 파일로 남는다. `savelib` 이
`Map` 레코드를 읽어 `tsa.maps.v1` 에 넣는다.

    세이브에 있는 지역  = 내가 연 곳
    세이브에 없는 지역  = 아직 못 가는 곳

**게임이 업데이트돼 새 지역이 열리면, 세이브를 다시 넣는 것만으로 저절로
풀린다.** 코드를 고칠 일이 없다. 이게 기본이다.

## ② 세이브가 없으면 — 빈 지역은 숨긴다

도시도 탐색지도 퀘스트도 하나도 없는 지역은 «다음 업데이트 자리» 다.
데이터만 있고 내용이 없으므로 판정에 근거가 있다.

    2026-08 기준: 발케인 지방 (도시 0 · 탐색지 0 · 퀘스트 0)

이건 `build_meta.py` 가 셀 수 있으니 손으로 적지 않는다.

## ③ 그래도 남는 것 — 손으로 적는 목록 하나

내용은 다 있는데 아직 서비스에 안 열린 지역이 있다. 데이터만 보고는
알 수 없다(퀘스트로 열리게 돼 있고, 그 퀘스트도 데이터에 있다).

    2026-08 기준: 칼레바스 고원
      · 도시 2(자르간트·노르그레이) · 탐색지 46 · 퀘스트 180 — 내용은 완성돼 있다
      · 트레반의 마을의 6_5_81_NextMapTower(Lv30·평판34) 가 여는 것으로 돼 있다

**고칠 곳은 `full/build_meta.py` 의 `LOCKED_REGIONS` 한 줄뿐이다.**
거기서 나온 값이 `meta.json` / `codex.json` 의 `locked` 로 실려 모든
페이지가 같은 것을 본다 — 페이지마다 지역 이름을 적어 두지 않는다.

세이브를 넣으면 ①이 ③을 덮으므로, 실제로는 세이브를 쓰는 순간
이 목록은 무시된다.

## 어디에 적용되는가

- 퀘스트 색인 — 도시·퀘스트
- 도감 — 탐색지 · 몬스터 · 아이템 · 지역 고르개
- 편람 — 종족의 주점 · 도시 시설 · 지도 · 뽑기 계산기 · 능력치 계산기의 장비 목록

「미출시 지역 보기」를 켜면 전부 다시 나온다.
